<?xml version="1.0" encoding="utf-8"?>
<!-- Cleartext HTTP allowed only for the Android emulator's host-loopback alias, so the app
     can talk to `manage.py runserver` during development. The production Render URL is
     HTTPS and unaffected by this. -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
